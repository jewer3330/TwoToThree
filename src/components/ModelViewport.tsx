import {useEffect,useRef,useState} from 'react';import * as THREE from 'three';import {OrbitControls} from 'three/addons/controls/OrbitControls.js';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';
export default function ModelViewport({url,onStats,onSelect}:{url:string;onStats:(s:Record<string,number|string>)=>void;onSelect:(name:string)=>void}){const host=useRef<HTMLDivElement>(null),api=useRef<any>(null),[error,setError]=useState(''),[loading,setLoading]=useState(0);useEffect(()=>{if(!host.current)return;const el=host.current,scene=new THREE.Scene();scene.background=new THREE.Color(0x111820);const camera=new THREE.PerspectiveCamera(30,el.clientWidth/el.clientHeight,.01,1000);camera.position.set(3,2.3,6);const renderer=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(el.clientWidth,el.clientHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.shadowMap.enabled=true;el.appendChild(renderer.domElement);const controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;scene.add(new THREE.HemisphereLight(0xd9e9ff,0x263141,2.5));const key=new THREE.DirectionalLight(0xffffff,4);key.position.set(4,7,5);key.castShadow=true;scene.add(key);const rim=new THREE.DirectionalLight(0x755cff,3);rim.position.set(-5,4,-4);scene.add(rim);const grid=new THREE.GridHelper(20,30,0x334255,0x202a36);scene.add(grid);let model:THREE.Object3D|undefined,auto=false,gray=false,wire=false,disposed=false;const originals=new Map<THREE.Mesh,THREE.Material|THREE.Material[]>();const grayMat=new THREE.MeshStandardMaterial({color:0xaab2bd,roughness:.72});new GLTFLoader().load(url,g=>{if(disposed)return;model=g.scene;let vertices=0,triangles=0,materials=new Set<THREE.Material>(),textures=new Set<THREE.Texture>();model.traverse(o=>{if((o as THREE.Mesh).isMesh){const m=o as THREE.Mesh; originals.set(m,m.material);const geo=m.geometry;vertices+=geo.attributes.position?.count||0;triangles+=geo.index?geo.index.count/3:(geo.attributes.position?.count||0)/3;(Array.isArray(m.material)?m.material:[m.material]).forEach(mat=>{materials.add(mat);Object.values(mat).forEach(v=>v?.isTexture&&textures.add(v))})}});const box=new THREE.Box3().setFromObject(model),size=box.getSize(new THREE.Vector3()),center=box.getCenter(new THREE.Vector3()),scale=4/Math.max(size.y,.001);model.scale.setScalar(scale);model.position.set(-center.x*scale,-box.min.y*scale,-center.z*scale);scene.add(model);controls.target.set(0,2,0);controls.update();onStats({vertices:Math.round(vertices),triangles:Math.round(triangles),materials:materials.size,textures:textures.size});setLoading(100)},e=>e.total&&setLoading(Math.round(e.loaded/e.total*100)),e=>setError(`GLB 加载失败：${e instanceof Error?e.message:'文件不可解析'}`));const view=(x:number,y:number,z:number)=>{auto=false;camera.position.set(x,y,z);controls.target.set(0,2,0);controls.update()};api.current={view,toggleAuto:()=>auto=!auto,toggleGray:()=>{gray=!gray;originals.forEach((mat,mesh)=>mesh.material=gray?grayMat:mat)},toggleWire:()=>{wire=!wire;originals.forEach(mat=>(Array.isArray(mat)?mat:[mat]).forEach(m=>{(m as THREE.MeshStandardMaterial).wireframe=wire;m.needsUpdate=true}))},screenshot:()=>{const a=document.createElement('a');a.download='model-review.png';a.href=renderer.domElement.toDataURL('image/png');a.click()}};const ray=new THREE.Raycaster(),pointer=new THREE.Vector2();const click=(e:MouseEvent)=>{if(!model)return;const r=renderer.domElement.getBoundingClientRect();pointer.set((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height)*2+1);ray.setFromCamera(pointer,camera);onSelect(ray.intersectObject(model,true)[0]?.object.name||'Root')};renderer.domElement.addEventListener('click',click);const resize=()=>{camera.aspect=el.clientWidth/el.clientHeight;camera.updateProjectionMatrix();renderer.setSize(el.clientWidth,el.clientHeight)};const ro=new ResizeObserver(resize);ro.observe(el);renderer.setAnimationLoop(()=>{if(auto&&model)model.rotation.y+=.004;controls.update();renderer.render(scene,camera)});return()=>{disposed=true;ro.disconnect();renderer.setAnimationLoop(null);renderer.domElement.removeEventListener('click',click);renderer.dispose();grayMat.dispose();el.replaceChildren()}},[url]);return <div className="viewport"><div ref={host}/>{loading<100&&!error&&<div className="viewport-loading"><span style={{width:`${loading}%`}}/>正在加载真实 GLB · {loading}%</div>}{error&&<div className="viewport-error">{error}<small>系统不会回退到占位模型。</small></div>}<div className="viewport-tools"><button onClick={()=>api.current?.view(0,2.1,7)}>正面</button><button onClick={()=>api.current?.view(7,2.1,0)}>侧面</button><button onClick={()=>api.current?.view(0,2.1,-7)}>背面</button><button onClick={()=>api.current?.view(5,2.5,5)}>3/4</button><i/><button onClick={()=>api.current?.toggleAuto()}>自动旋转</button><button onClick={()=>api.current?.toggleGray()}>灰模</button><button onClick={()=>api.current?.toggleWire()}>线框</button><button onClick={()=>api.current?.screenshot()}>截图</button></div></div>}
