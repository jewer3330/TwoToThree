import {useCallback,useEffect,useState} from 'react';import {Box,MessageSquarePlus,Paperclip,Send,Wrench,XCircle} from 'lucide-react';import {useNavigate,useParams} from 'react-router-dom';import {api} from '../api';import type {Version,VersionComment} from '../types';import ModelViewport,{type AnnotationSnapshot} from '../components/ModelViewport';import {PageHeader} from '../App';
const empty={title:'',description:'',category:'geometry',severity:'normal',recommendedRoute:'reference_regeneration'};
export default function ReviewPage(){const {projectId=''}=useParams(),nav=useNavigate();const [versions,setVersions]=useState<Version[]>([]),[active,setActive]=useState<Version>(),[comments,setComments]=useState<VersionComment[]>([]),[selected,setSelected]=useState<string[]>([]),[current,setCurrent]=useState<VersionComment>(),[draft,setDraft]=useState<any>(empty),[anchor,setAnchor]=useState<AnnotationSnapshot>(),[reply,setReply]=useState(''),[error,setError]=useState('');
 const load=useCallback((vid:string)=>api.comments(vid).then(setComments).catch(e=>setError(e.message)),[]);useEffect(()=>{api.versions(projectId).then(v=>{setVersions(v);setActive(v[0]);if(v[0])load(v[0].id)}).catch(e=>setError(e.message))},[projectId,load]);
 const save=async()=>{if(!active)return;try{const c=await api.createComment(active.id,{...draft,...anchor});setComments(x=>[c,...x]);setDraft(empty);setAnchor(undefined);setCurrent(c)}catch(e){setError((e as Error).message)}};
 const update=(c:VersionComment)=>{setComments(x=>x.map(v=>v.id===c.id?c:v));setCurrent(c)};if(!active)return <div className="loading"><span/>正在读取版本…</div>;
 return <><PageHeader eyebrow={`项目 ${projectId.slice(0,8)} · 版本验收`} title="模型预览与 Comment 评审" description="一个 Comment 只描述一个问题；保存不会启动任务。双击模型可记录标注位置、相机视角和截图。"/><div className="comment-review-layout"><aside className="panel version-panel"><h2>版本历史</h2>{versions.map(v=><button className={active.id===v.id?'active':''} key={v.id} onClick={()=>{setActive(v);setCurrent(undefined);setSelected([]);load(v.id)}}><Box/><span><b>v{String(v.number).padStart(3,'0')}</b><small>{v.label}</small></span></button>)}<h2>筛选</h2><p>当前版本 {comments.length} 条 · 已选 {selected.length} 条</p></aside><section className="review-center">{active.model?<ModelViewport url={active.model.url} onStats={()=>{}} onSelect={()=>{}} onAnnotate={setAnchor}/>:<div className="viewport-error">当前版本没有 GLB</div>}<div className="download-bar"><button onClick={()=>setCurrent(undefined)}><MessageSquarePlus/>新建 Comment</button><button disabled={!selected.length} onClick={()=>nav(`/revisions/new/${active.id}?comments=${selected.join(',')}`)}><Wrench/>创建修订任务（{selected.length}）</button></div></section><aside className="panel comments-panel"><h2>Comments</h2><div className="comment-list">{comments.map(c=><article className={current?.id===c.id?'active':''} key={c.id} onClick={()=>setCurrent(c)}><input type="checkbox" checked={selected.includes(c.id)} onChange={e=>{e.stopPropagation();setSelected(x=>e.target.checked?[...x,c.id]:x.filter(i=>i!==c.id))}}/><b>#{c.number} {c.title}</b><small>{c.severity} · {c.status}</small></article>)}</div>{current?<div className="comment-detail"><h3>#{current.number} {current.title}</h3><p>{current.description}</p><small>{current.category} · {current.recommendedRoute}</small>{current.screenshotUrl&&<img src={current.screenshotUrl}/>}<div className="reply-thread">{current.replies.map(r=><p key={r.id}><b>{r.authorType}</b>{r.body}</p>)}</div><div className="inline-input"><input value={reply} onChange={e=>setReply(e.target.value)} placeholder="回复…"/><button onClick={async()=>{if(reply.trim()){update(await api.replyComment(current.id,reply));setReply('')}}}><Send/></button></div><label className="attach"><Paperclip/>上传辅助参考图<input type="file" accept="image/*" onChange={async e=>{const f=e.target.files?.[0];if(f)update(await api.attachComment(current.id,'front',f))}}/></label><button onClick={async()=>update(current.status==='closed'?await api.reopenComment(current.id):await api.closeComment(current.id))}><XCircle/>{current.status==='closed'?'重新打开':'关闭 Comment'}</button></div>:<div className="comment-editor"><h3>新建独立 Comment</h3>{anchor&&<div className="notice success">已记录 {anchor.meshName} 标注点与当前视角</div>}<label>标题<input value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})}/></label><label>详细说明<textarea value={draft.description} onChange={e=>setDraft({...draft,description:e.target.value})}/></label><div className="field-pair"><label>类型<select value={draft.category} onChange={e=>setDraft({...draft,category:e.target.value})}><option value="geometry">几何</option><option value="identity">身份特征</option><option value="intersection">穿插</option><option value="material">材质</option><option value="color">颜色</option><option value="other">其他</option></select></label><label>严重程度<select value={draft.severity} onChange={e=>setDraft({...draft,severity:e.target.value})}><option value="blocking">阻断</option><option value="important">重要</option><option value="normal">一般</option><option value="note">备注</option></select></label></div><label>处理路线<select value={draft.recommendedRoute} onChange={e=>setDraft({...draft,recommendedRoute:e.target.value})}><option value="reference_regeneration">参考图重生成</option><option value="blender_automatic">Blender 自动</option><option value="manual">人工处理</option><option value="not_configured">未配置</option></select></label><button className="button" disabled={!draft.title.trim()||!draft.description.trim()} onClick={save}>保存 Comment</button></div>}{error&&<div className="notice danger">{error}</div>}</aside></div></>}
