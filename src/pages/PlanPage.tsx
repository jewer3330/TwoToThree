import {useEffect,useState} from 'react';import {AlertTriangle,Box,CheckCircle2,Cpu,FileBox,Image,Layers3,Play,Settings2} from 'lucide-react';import {useNavigate,useParams} from 'react-router-dom';import {api} from '../api';import {Button,PageHeader} from '../App';
const flow=[['主体分析',Cpu],['Hunyuan3D',Box],['GLB 检查',CheckCircle2],['四视图渲染',Image],['材质处理',Layers3],['网页优化',FileBox]] as const;
export default function PlanPage(){const {projectId=''}=useParams(),nav=useNavigate();const [plan,setPlan]=useState<any>();const [error,setError]=useState('');useEffect(()=>{api.plan(projectId).then(setPlan).catch(e=>setError(e.message))},[projectId]);const setQuality=(geometryQuality:string)=>setPlan({...plan,geometryQuality,textureResolution:geometryQuality==='standard'?0:geometryQuality==='high'?2048:4096,faceRefinement:geometryQuality==='ultra'});const start=async()=>{try{await api.updatePlan(projectId,plan);const job=await api.createJob(projectId);nav(`/jobs/${job.id}`)}catch(e){setError((e as Error).message)}};if(!plan)return <div className="loading"><span/>正在生成转换方案…</div>;return <><PageHeader eyebrow="步骤 3 / 3 · 方案确认" title="转换方案确认" description="质量等级同时锁定几何、纹理和脸部精修能力。"/><div className="plan-layout"><section><div className="panel pipeline"><div className="section-title"><h2>所选转换流程</h2><span>推荐路线</span></div><div className="pipeline-flow">{flow.map(([label,I],i)=><div className="flow-step" key={label}><em>{i+1}</em><I/><b>{label}</b></div>)}</div><div className="fallback"><span>质量路线</span><b>标准 256 / 无纹理</b> → <b>高 384 / 2K</b> → <b>超高 512 / 脸部精修 / 4K</b></div></div><div className="metric-cards"><div><Cpu/><small>主生成后端</small><b>Hunyuan3D</b></div><div><Box/><small>几何分辨率</small><b>{{standard:256,high:384,ultra:512}[plan.geometryQuality as 'standard'|'high'|'ultra']}</b></div><div><Image/><small>纹理分辨率</small><b>{plan.textureResolution?`${plan.textureResolution}²`:'无纹理'}</b></div><div><Layers3/><small>脸部精修</small><b>{plan.faceRefinement?'启用':'关闭'}</b></div></div><div className="panel outputs"><h2>交付内容</h2>{['可验收 GLB','正面 / 左 3/4 / 侧面 / 背面渲染','模型统计与质量报告','配置快照与阶段日志'].map(x=><span key={x}><CheckCircle2/>{x}</span>)}</div></section><aside><div className="panel risk-panel"><h2><AlertTriangle/> 已知限制与近似</h2>{plan.limitations.map((x:string)=><p key={x}>• {x}</p>)}</div><div className="panel settings-panel"><h2><Settings2/> 质量等级</h2><label>主后端<select value={plan.primaryBackend} onChange={e=>setPlan({...plan,primaryBackend:e.target.value})}><option value="hunyuan3d">Hunyuan3D</option><option value="sf3d">Stable Fast 3D</option><option value="triposr">TripoSR</option></select></label><label>质量路线<select value={plan.geometryQuality} onChange={e=>setQuality(e.target.value)}><option value="standard">标准：256 几何，无纹理</option><option value="high">高：384 几何＋2048 纹理</option><option value="ultra">超高：512 几何＋脸部精修＋4096 纹理</option></select></label><label>纹理策略<input value={plan.textureResolution?`三视图投射 · ${plan.textureResolution}×${plan.textureResolution}`:'关闭'} disabled/></label><label className="check"><input type="checkbox" checked={plan.segmentationRequired} onChange={e=>setPlan({...plan,segmentationRequired:e.target.checked})}/> 要求分件</label><label className="check"><input type="checkbox" checked={plan.rigRequired} onChange={e=>setPlan({...plan,rigRequired:e.target.checked})}/> 需要骨骼</label></div></aside></div>{error&&<div className="notice danger">{error}</div>}<div className="sticky-actions"><Button kind="secondary" onClick={()=>nav(`/validation/${projectId}`)}>返回修改</Button><span>配置确认后不可原地修改</span><Button onClick={start}><Play/> 确认并开始转换</Button></div></>}
